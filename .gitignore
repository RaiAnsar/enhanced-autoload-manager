# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Temporary files
*.tmp
*.temp

# Secrets — never commit SVN/WordPress.org credentials
credentials.md
**/credentials.md

# WordPress core (kept on disk for local testing only, not part of the plugin repo)
wordpress/

# SVN working-copy internals (the deploy checkout lives here; its metadata is not git's job)
.svn/
**/.svn/
# SVN release-tag snapshots are managed by SVN, not git (only trunk/ is tracked here)
enhanced-autoload-manager-svn/tags/